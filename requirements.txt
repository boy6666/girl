# ============ Web 伴侣后台（V1.5+，真实运行所需） ============
# Web 后台（web.main）实际只需要下面这几个。表情包下载走标准库 urllib，
# 作息/日记/梦/反思/记忆图谱全是标准库，无需任何 AI/向量依赖。

fastapi==0.110.0
uvicorn[standard]==0.27.1
jinja2==3.1.3
pyyaml==6.0.1

# ============ 开发 / 测试 ============
pytest==8.0.0
pytest-asyncio==0.23.4
httpx>=0.24        # starlette TestClient 需要

# ============ 旧引擎占位（不用于 Web 后台，装时跳过即可） ============
# openai / chromadb / ollama / sqlalchemy / aiosqlite / python-dotenv / aiofiles
# 这些属于已被 OpenClaw 接管的旧 app/ 引擎与记忆系统，当前 main 入口不进。
# 需要再按需装，别被旧 pin 卡住（旧 ollama==0.1.41 版本号本就不存在，已删除）。
