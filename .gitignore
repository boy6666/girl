# === Runtime / secrets — never commit ===
.env
.env.*
!.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# Runtime data (记忆/数据库/日志 — 100% 本地，不上传)
data/*.sqlite
data/*.db
data/**/*.sqlite
data/**/*.db
*.log
girl_workspace/memory/
girl_workspace/**/memory/

# Web 后台运行态（滑块当前值，不入库）
web/personality.yaml

# 主动状态机运行态 + 小语的生活（100% 本机私人数据，不入库）
data/state.json
data/life_journal.md
data/life_content.yaml

# 第三方情绪标注数据集（EmoTag / Emoji Sentiment Ranking，本地自用，不入库）
data/emoji/

# 表情包下载（本机运行态，不入库）
data/media/


# OpenClaw 工作区状态标记（自动生成）
girl_workspace/openclaw-workspace-state.json

# 非必要文档不入 git —— 只推代码 + README（用户 git 策略）
docs/

# Node / OpenClaw
node_modules/
*.openclaw.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
